package com.vapasi;

public class Main {

    public static void main(String[] args) {
        // Add is the method from Collection interface which all of them implements

        ListInterfaceImplementations listInterfaceImplementations = new ListInterfaceImplementations();
        listInterfaceImplementations.execute();

        QueueInterfaceImplementations queueInterfaceImplementations = new QueueInterfaceImplementations();
        queueInterfaceImplementations.execute();
    }
}
